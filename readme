#!/bin/bash
node --inspect --experimental-json-modules -e 'import("./Blik_2020_seed.js").then(seed=>seed.open("./Blik_2020_root.js","https"))';
exit 0
dsa
Execute this readme to seed.
Check to specify the protocol you need (http/https).
Package/bundle acquisition and testing are all implied, not separate.
(parameters.json is intercepted by "package.json" only for npm versioning).


itinerary#strong :
dragon#scan{height:1em;fill:green} crucial snail#scan{height:1em;fill:#795548} proposal apple#scan{height:1em;fill:#b71c1c} ready worm#scan{height:1em} bug


syntax_guide#strong :
#1#strong one line: one statement.
#2#strong break before block specifiers,
ternary, array operators ([]{}?:).
#3#strong indent blocks with one space.
sequence#strong : imports, exports, default export, internal functions, internal variables, expressions


corridor#scan{height:1em;fill:white}
root.js#strong
form#strong : html radio form to
select path offering module options.
default to input path, switching that of "/" to "module".
react#strong : reproduction of "form" in react's dialect
mind#strong : hmac-key-authenticated identity.
mind.get#strong :
authority#strong : for randomstring-session token.
order#strong : scheduled external request.
stuff#strong for orderable shop items.
store.get#strong :
mongo.get#strong :
matrix.get#strong :
modules#strong : module import hierarchy from babel-parser's abstract syntax tree
peripheries#strong : module

